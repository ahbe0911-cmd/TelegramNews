# نبض خبر — نسخه مستقل TDLib برای چند کانال عمومی

این نسخه از پروژه دیگر به Bot API، وب‌هوک تلگرام، کانال اختصاصی شما، Cloudflare Workers یا Firebase برای نمایش اخبار وابسته نیست. TDLib به حساب **کاربر روی گوشی** وارد می‌شود و خبرهای کانال‌هایی را که کاربر انتخاب کرده می‌خواند.

## قابلیت‌های پیاده‌سازی‌شده

- ورود مستقیم روی گوشی: API ID و API Hash و سپس شماره تلفن، کد ورود و در صورت نیاز گذرواژه دومرحله‌ای.
- API ID و API Hash در فضای امن Android به‌وسیله flutter_secure_storage ذخیره می‌شوند. کد ورود و گذرواژه دومرحله‌ای ذخیره نمی‌شوند.
- افزودن چند کانال عمومی با نام کاربری یا لینک t.me/username. لمس «افزودن و عضویت» **حساب تلگرام شما را عضو کانال می‌کند** تا پیام‌های جدید دریافت شوند.
- دریافت تا ۴۰ پیام اخیر هر کانال در شروع و تازه‌سازی؛ دریافت updateNewMessage در زمان اجرای برنامه.
- نمایش خبرها در یک فهرست فارسی با تاریخ شمسی، جست‌وجوی محلی، تیتر، متن، تصویر قابل دانلود و لینک به پست در تلگرام.
- نگه‌داری نشست حساب و تاریخچه از طریق دیتابیس داخلی TDLib در فضای اختصاصی اپ اندروید.

## ساخت APK مخصوص A54

به [GitHub Actions → Build TDLib A54 APK](https://github.com/ahbe0911-cmd/TelegramNews/actions/workflows/build-tdlib-a54.yml) بروید؛ Run workflow را انتخاب کنید. بعد از موفقیت کامل build، artifact با نام NabzKhabar-TDLib-A54 را دریافت کنید. فایل APK داخل ZIP است. شناسه بسته نسخه جدید ir.channel.telegram_tdnews است و از اپ قدیمی جدا نصب می‌شود.

فایل اصلی این نسخه lib/td_news_main.dart است و فایل موتور TDLib در lib/td_news_engine.dart قرار دارد. گردش‌کار جدید از Flutter stable، فایل‌های فونت موجود در assets/fonts و خروجی android-arm64 استفاده می‌کند.

## استفاده روی گوشی

۱. از https://my.telegram.org/apps شناسه API ID و API Hash اختصاصی خودتان را دریافت کنید. این شناسه‌ها، شماره، کد ورود و گذرواژه را **در گیت‌هاب یا گفتگو منتشر نکنید**.
۲. APK جدید را نصب و باز کنید. API ID و API Hash را داخل اپ وارد کنید.
۳. کد ورود ارسالی تلگرام و در صورت لزوم گذرواژه دومرحله‌ای را **در خود اپ** وارد کنید.
۴. آدرس یک کانال عمومی مانند t.me/ExampleNews را وارد و روی «افزودن و عضویت» بزنید. با این کار حساب تلگرام شما عضو کانال می‌شود؛ این موضوع به‌معنی عضو کردن ربات یا دریافت اخبار تمام کانال‌ها نیست.
۵. کانال‌های دیگر را نیز اضافه کنید. آخرین پست‌ها را در یک فهرست خواهید دید.

## محدودیت‌ها و وضعیت آزمون

- دریافت پیام‌های جدید تا وقتی برنامه و نشست TDLib فعال هستند انجام می‌شود. اندروید ممکن است اجرای برنامه در پس‌زمینه را متوقف کند؛ **اعلان فوریِ تضمین‌شده هنگام بسته بودن کامل برنامه در این نسخه فعال نشده است**. هنگام باز کردن دوباره اپ، تا ۴۰ پیام آخر هر کانال دوباره خوانده می‌شوند.
- ویدئوهای پشتیبانی‌شده با لمس دکمه پخش **داخل کارت صفحه خبرها** اجرا می‌شوند. پخش خودکار و دانلود پیش‌دستانه خاموش هستند؛ ویدئو پس از دریافت فایل کامل از تلگرام شروع می‌شود. هنگام جابه‌جایی صفحه، پیمایش فهرست یا رفتن برنامه به پس‌زمینه، پخش متوقف می‌شود.
- محتوای متن در کارت و پنجره جزئیات با چینش Justify و جهت راست‌به‌چپ نشان داده می‌شود. این کار ظاهر متن را مرتب می‌کند، اما مفهوم و محتوا را بازنویسی نمی‌کند.
- PDF به‌صورت جدا در نمایشگر داخلی باز می‌شود؛ کدک‌های غیرقابل پخش و فایل‌های رمزگذاری‌شده ممکن است نیاز به باز کردن در تلگرام داشته باشند.
- V2Ray و افزونه آن از نسخه جدید حذف شده‌اند؛ اطلاعات حساب ذخیره‌شده قدیمی V2Ray نیز هنگام ورود برنامه پاک می‌شوند.
- **تمام قابلیت‌های WebSocket، TG WS Proxy و V2Ray از APK این نسخه حذف شده‌اند.** این نسخه از اتصال مستقیم تلگرام استفاده می‌کند و گزینه پراکسی در تنظیمات ندارد. اگر نسخه قدیمی داخل TDLib پراکسی ذخیره کرده باشد، برنامه هنگام ورود آن را غیرفعال می‌کند.
- بالای صفحه اخبار، کارت ساعت ۱۲ ساعته و تاریخ شمسی قرار دارد. ناوبری پایین شامل «خبرها» و «تنظیمات» است.
- فهرست خبرها با ListView.builder به‌صورت تنبل ساخته می‌شود؛ تاریخچه محلی TDLib ابتدا نمایش داده می‌شود و سپس شبکه تازه‌سازی می‌شود. فقط تصاویر بندانگشتی کارت‌های قابل‌مشاهده بارگذاری می‌شوند؛ فایل ویدئو یا PDF در حالت عادی دانلود نمی‌شود.
- پست‌های ویدئو، عکس، PDF و اسناد معمول، از جمله فایل‌های APK، دکمه دانلود دارند. با لمس، فایل اصلی از تلگرام دریافت می‌شود و سپس در Android 10 به بعد داخل Downloads/NabzKhabar ذخیره می‌شود. در اندروید قدیمی‌تر، فایل در پوشه Downloads اختصاصی برنامه قرار می‌گیرد.
- ویدئو پس از لمس کارت و تکمیل دریافت فایل در همان صفحه اصلی پخش می‌شود؛ تصویر پیش‌نمایش و درصد دریافت تا آماده‌شدن ویدئو نشان داده می‌شوند. برای فایل‌های بزرگ یا اینترنت کند، شروع پخش همچنان ممکن است زمان ببرد؛ پخش استریم تضمین‌شده هنوز پیاده‌سازی نشده است.
- PDF در نمایشگر داخلی قابل خواندن است و عکس را می‌توان با بزرگ‌نمایی باز کرد. برای فایل‌های ناسازگار با نمایشگر داخلی، دانلود فایل یا مشاهده پست در تلگرام جایگزین است.
- سرعت افزودن منبع خبری با انتقال بارگذاری تاریخچه به پس‌زمینه و دریافت محدود ۱۲ پست ابتدایی بهبود یافته است. تازه‌سازی دستی ۲۵ پست هر منبع را همزمان درخواست می‌کند.
- پیش‌نمایش کوچک عکس/ویدئو از خود پیام یا بندانگشتی کم‌حجم دریافت می‌شود؛ سرعت واقعی دانلود به شبکه، حجم فایل و پاسخ تلگرام وابسته است.
- حذف یک کانال از فهرست اپ، شما را در حساب تلگرام از آن کانال خارج نمی‌کند.
- تصاویر و اخبار روی همین گوشی پردازش می‌شوند و برای نمایش خودکار در گوشی‌های دیگر به سرور ارسال نمی‌شوند.
- پکیج tdlib نسخه 1.6.0 از کتابخانه بومی قدیمی‌تر استفاده می‌کند. گردش‌کار ساخت namespace پلاگین قدیمی را برای Gradle جدید اصلاح می‌کند. عملکرد نسخه جدید در گوشی واقعی و نتیجه نهایی GitHub Actions باید جداگانه تأیید شوند. فایل APK را پیش از موفقیت کامل Actions «آماده» تلقی نکنید.
- اگر حساب تلگرام احراز هویت اضافی دیگری مثل تأیید از دستگاه دیگر درخواست کند، آن را در تلگرام انجام دهید. از ارسال کد ورود یا رمز به شخص دیگر خودداری کنید.

منابع اصلی: https://core.telegram.org/tdlib/getting-started ، https://core.telegram.org/api/obtaining_api_id ، https://pub.dev/packages/tdlib
